# Deprecated

Dead stubs, superseded prototypes, and one vendor sample file, moved here during 2026-27 season prep. None of these were referenced by anything else in the codebase.

- `AutonStrategies/CloseStrategy.java` — empty stub; the strategy lambda body was never filled in.
- `AutonStrategies/FarStrategy.java`, `BasicApriltagTracking.java` — early AprilTag-tracking prototypes, superseded by the (archived, see `PastSeasons/2025-2026/`) `ATagL1Strategy`/`ATagL2Strategy`.
- `OpModes/lime.java` — an unmodified FTC SDK/Limelight vendor sample OpMode that had been copied into `TeamCode/` rather than left in `FtcRobotController/`'s samples folder. Registered as `@TeleOp(name="Sensor: Limelight3A")`; kept for reference, not team-authored.
